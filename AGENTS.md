# Codex 프로젝트 규칙

이 프로젝트는 요구사항·제품 디자인·모듈/시스템 테스트를 Codex가, 제품 코드
구현과 단위 테스트를 Claude가 담당하는 이중 에이전트 워크플로를 사용한다.

## 반드시 먼저 할 일

작업을 시작하기 전에 다음 파일을 읽는다. 이 파일이 전체 규칙의 정본이다.

    workflow/rules/codex.md

아래 요약은 그 파일을 읽지 못한 경우를 위한 최소 안전장치일 뿐이다.
요약과 정본이 다르면 정본을 따른다.

---

## 최소 안전장치

### Codex 소유 (수정 가능)

- `docs/requirements/`
- `docs/design/`, `design-assets/`
- `tests/module/`, `tests/system/`

### Claude 소유 (읽기만, 절대 수정 금지)

- `src/` 제품 코드
- `tests/unit/` 단위 테스트
- `workflow/` 워크플로 스크립트와 설정

제품 코드에서 버그를 발견해도 **직접 고치지 않고 보고만 한다.**

### 강제 방식

Codex는 전권 샌드박스로 실행된다. 능력 제한은 없다. 대신 실행 후 두 겹의
가드가 소유권을 검증한다.

1. git 변경 경로가 단계별 허용 목록을 벗어나면 자동 복원하고 실행 결과를
   무효 처리한다.
2. `src/`와 `tests/unit/`의 파일별 해시를 실행 전후로 비교한다. gitignore된
   파일까지 포함한다.

가드에 걸리면 그 실행은 전부 폐기된다.

### 요구사항 ID

| 종류 | 형식 |
|---|---|
| 시스템 | `SYS-FR-###` / `SYS-NFR-###` / `SYS-AC-###` |
| 모듈 | `MOD-<모듈>-FR-###` / `-NFR-###` / `-AC-###` |

모든 모듈 요구사항은 상위 시스템 요구사항 ID를 기록한다.
모든 시스템 요구사항은 구현 모듈 ID를 기록한다.
양쪽 다 없으면 추적성 파서가 고아로 판정해 실패시킨다.

### 테스트

`tests/module/<모듈>/`은 `MOD-<모듈>-AC-###`를,
`tests/system/`은 `SYS-AC-###`를 검증한다.

**테스트 이름 또는 속성에 검증 대상 인수 조건 ID를 반드시 포함한다.**
없으면 고아로 판정된다.

테스트 명령은 `workflow/config.json`에 정의된 것만 사용한다.
테스트 실행을 생략하고 결과를 추측하지 않는다.
실패한 테스트를 삭제, 비활성화, 기대값 하향하지 않는다.

### 상태

현재 대상은 `workflow/state.json`에서 읽는다.
상태 파일은 직접 수정하지 않는다. 래퍼가 갱신한다.
